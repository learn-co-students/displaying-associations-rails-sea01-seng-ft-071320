<h1><%= @category.name %></h1>

<h3><%= pluralize(@category.posts.count, 'Post') %> </h3>

<ul>
  <% @category.posts.each do |p| %>
    </i><%= link_to p.title, post_path(p)%></li>
    <i>loop me <br></i>
<% end %>
  <b> | cat show page </b>
</ul>
<%# byebug %>